
# 🏦 Система управления банковскими картами

REST API для управления банковскими картами с аутентификацией JWT и ролевой моделью доступа.

## 🚀 Технологии

- **Java 17+**
- **Spring Boot 3.2.0**
- **Spring Security + JWT**
- **Spring Data JPA**
- **PostgreSQL**
- **Liquibase** (миграции БД)
- **Docker & Docker Compose**
- **Swagger/OpenAPI 3.0**
- **Maven**

## 📋 Функциональность

### 🔐 Аутентификация и авторизация
- JWT токены для аутентификации
- Роли: `ADMIN` и `USER`
- Регистрация новых пользователей
- Защищенные endpoints с ролевым доступом

### 💳 Управление картами
- **Администратор:**
    - Создание, блокировка, активация, удаление карт
    - Просмотр всех карт в системе
    - Управление статусами карт

- **Пользователь:**
    - Просмотр своих карт с пагинацией
    - Фильтрация карт по статусу
    - Запрос блокировки/активации карт
    - Просмотр баланса

### 🔄 Переводы между картами
- Переводы между собственными картами
- Валидация достаточности средств
- Проверка активности карт
- История транзакций

### 🛡️ Безопасность
- Шифрование номеров карт в БД
- Маскирование номеров карт при отображении (`**** **** **** 1234`)
- Валидация входных данных
- Защита от несанкционированного доступа

## 🗄️ Модель данных

### Сущности:
- **User** - пользователи системы
- **Card** - банковские карты
- **Transfer** - переводы между картами
- **Role** - роли пользователей (ADMIN, USER)

### Статусы карт:
- `ACTIVE` - активна
- `BLOCKED` - заблокирована
- `EXPIRED` - истек срок действия

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd bank_rest
```

### 2. Запуск через Docker Compose
```bash
docker-compose up -d
```

### 3. Или локальный запуск
```bash
# Запуск PostgreSQL
docker-compose up -d postgres

# Запуск приложения
mvn spring-boot:run
```

## 📚 API Документация

После запуска приложения документация доступна по адресам:

- **Swagger UI:** http://localhost:8080/api/swagger-ui.html
- **OpenAPI Spec:** http://localhost:8080/api/v3/api-docs

## 🔑 Тестовые пользователи

Создаются автоматически при первом запуске:

### Администратор
- **Username:** `admin`
- **Password:** `$2a$12$7BcQY6Y4X8z2wVkLp9n8PeOVD5eQ2rJkK6YbWzXpLmNvGtRqS1T2u`
- **Роль:** `ADMIN`

### Обычный пользователь
- **Username:** `user1`
- **Password:** `$2a$12$9DfQwY6X8pLmNvGtRqS1T2uPeOVD5eQ2rJkK6YbWzXpLmNvGtRqS1T2`
- **Роль:** `USER`

## 📡 API Endpoints

### Аутентификация
- `POST /auth/login` - вход в систему
- `POST /auth/register` - регистрация пользователя

### Карты (пользователь)
- `GET /cards` - список карт пользователя
- `GET /cards/{id}` - информация о карте
- `PUT /cards/{id}/block` - блокировка карты
- `PUT /cards/{id}/activate` - активация карты

### Карты (администратор)
- `GET /admin/cards` - все карты в системе
- `POST /admin/cards` - создание новой карты
- `DELETE /admin/cards/{id}` - удаление карты
- `PUT /admin/cards/{id}/status` - изменение статуса карты

### Переводы
- `POST /transfers` - перевод между картами

## 🗃️ Миграции базы данных

Система использует Liquibase для управления миграциями:
- Файлы миграций: `src/main/resources/db/migration/`
- Автоматическое применение при запуске
- Версионность изменений схемы БД

## 🧪 Тестирование

Запуск тестов:
```bash
mvn test
```

Покрытие тестами:
- Unit-тесты сервисов
- Интеграционные тесты контроллеров
- Тесты безопасности

## 🔧 Конфигурация

Основные настройки в `application.yml`:

```yaml
server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_db
    username: bank_user
    password: bank_pass

jwt:
  secret: bankCardManagementSecretKey2024
  expiration: 86400000
```

## 🐳 Docker

### Запуск всех сервисов
```bash
docker-compose up --build
```

### Остановка
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f app
```

## 🛠️ Разработка

### Сборка проекта
```bash
mvn clean compile
```

### Запуск с горячей перезагрузкой
```bash
mvn spring-boot:run
```

### Создание JAR файла
```bash
mvn clean package
```
